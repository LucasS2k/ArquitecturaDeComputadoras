org 1000h
STRING db "texto de ejemplo"
FIN db ?
org 2000h
mov bx, offset STRING
lazo: mov al, [bx]
cmp al, 'A' ;al - 65
js siguiente
mov ah, 'Z' 
cmp ah, al ;97 - al
js siguiente
sub al, 32 ; o 20h es lo mismo
mov [bx], al
siguiente: inc bx
cmp bx, offset fin
jnz lazo
hlt
END